version: 0.2

phases:
  install:
    commands:
      - which java 
      - javac ready.java
      - java sum 
  pre_build:
    commands:
      - echo install
  build:
    commands:
      - echo buildci
      - echo testci
  post_build:
    commands:
      - echo name 